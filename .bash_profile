export PATH="${HOME}/bin:${PATH}"

[[ -d "/opt/adr-tools/src" ]] && PATH="${PATH}:/opt/adr-tools/src"

# This file is sourced by bash for login shells.  The following line
# runs your .bashrc and is recommended by the bash info pages.
[[ -f ~/.bashrc ]] && . ~/.bashrc
